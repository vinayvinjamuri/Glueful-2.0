/* =========================================================
   GLUEFUL RESUME STUDIO — FIXED-PAGE EDITOR RENDERER V5.1
   PDF artwork remains the visual source of truth.
   Text overlays are strictly transparent until edited and knockout
   masks are hidden by CSS authority so they cannot cover nearby PDF text.
   ========================================================= */
(function(){
'use strict';
const M=()=>window.gluefulResumeLayoutModel;
const CSS=`
.glueful-fixed-pages{position:relative;display:flex;flex-direction:column;gap:24px;align-items:center;width:max-content;min-width:100%;box-sizing:border-box;padding:0 8px 24px;overflow:visible;transform-origin:top left}
.glueful-fixed-page{position:relative;flex:0 0 auto;overflow:hidden;background:#fff;box-shadow:0 2px 14px rgba(0,0,0,.12);box-sizing:border-box;max-width:none}
.glueful-fixed-bg{position:absolute!important;inset:0!important;width:100%!important;height:100%!important;object-fit:fill!important;z-index:0!important;pointer-events:none!important;user-select:none!important;display:block!important;margin:0!important;padding:0!important;border:0!important}
.glueful-fixed-mask{position:absolute!important;box-sizing:border-box!important;background:#fff!important;z-index:2!important;pointer-events:none!important;visibility:hidden!important}
.glueful-fixed-mask.glueful-fixed-mask-visible{visibility:visible!important}
.glueful-fixed-text{position:absolute!important;box-sizing:border-box!important;margin:0!important;padding:0!important;background:transparent!important;color:transparent!important;overflow:hidden!important;white-space:pre-wrap!important;word-break:normal!important;overflow-wrap:break-word!important;max-width:none!important;outline:none!important;z-index:5!important;border:0!important;box-shadow:none!important;text-shadow:none!important;opacity:1!important}
.glueful-fixed-text.glueful-fixed-editing{color:#202124!important;background:transparent!important}
.glueful-fixed-text:focus{box-shadow:inset 0 0 0 1px rgba(68,112,255,.35)!important}
.glueful-fixed-text[data-editable="false"]{pointer-events:none!important}
.glueful-fixed-run{white-space:pre-wrap!important}
`;
function css(host){if(host.querySelector('style[data-glueful-fixed-page-v5-1]'))return;const s=document.createElement('style');s.dataset.gluefulFixedPageV51='1';s.textContent=CSS;host.prepend(s)}
function escapeHtml(v){return String(v??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/\"/g,'&quot;').replace(/'/g,'&#39;')}
function pageWidthOf(page){return Number(page.clientWidth||parseFloat(page.style.width)||794)}
function renderMask(page,mask){const n=document.createElement('div');n.className='glueful-fixed-mask';n.dataset.maskId=mask.id;if(mask.sourceBlockId)n.dataset.sourceBlockId=mask.sourceBlockId;const pw=pageWidthOf(page);n.style.left=`${Math.max(0,Math.min(pw,Number(mask.x||0)))}px`;n.style.top=`${Math.max(0,Number(mask.y||0))}px`;n.style.width=`${Math.max(0,Math.min(Number(mask.width||0),pw-Math.max(0,Number(mask.x||0))))}px`;n.style.height=`${Math.max(0,Number(mask.height||0))}px`;n.style.background=mask.color||'#fff';n.style.zIndex=String(mask.zIndex||2);page.appendChild(n);return n}
function safeBlockGeometry(page,block){const pageWidth=Number(block?.pageWidth||pageWidthOf(page));const x=Math.max(0,Math.min(Math.max(0,pageWidth-2),Number(block.x||0)));const requested=Math.max(4,Number(block.width||4));const width=Math.max(4,Math.min(requested,Math.max(4,pageWidth-x-2)));return{x,width}}
function revealBlock(page,block,node){node.classList.add('glueful-fixed-editing');const mask=page.querySelector(`.glueful-fixed-mask[data-source-block-id="${block.id}"]`);if(mask)mask.classList.add('glueful-fixed-mask-visible')}
function hideBlock(page,block,node){if(block.dirty)return;node.classList.remove('glueful-fixed-editing');const mask=page.querySelector(`.glueful-fixed-mask[data-source-block-id="${block.id}"]`);if(mask)mask.classList.remove('glueful-fixed-mask-visible')}
function shiftLaterBlocks(page,block,delta,options){if(!delta)return;for(const sibling of page.querySelectorAll('.glueful-fixed-text')){if(sibling===options.node)continue;const targetId=sibling.dataset.blockId;const target=window.gluefulResumeFixedPageState?.model?.pages?.find(p=>p.id===options.pageId)?.blocks?.find(b=>b.id===targetId);if(target&&target.y>block.y){target.y+=delta;sibling.style.top=`${target.y}px`;const mask=page.querySelector(`.glueful-fixed-mask[data-source-block-id="${target.id}"]`);if(mask)mask.style.top=`${target.y}px`}}}
function renderText(page,block,options){const n=document.createElement('div');n.className='glueful-fixed-text';n.dataset.blockId=block.id;n.dataset.editable=String(block.editable!==false);n.contentEditable=block.editable!==false?'true':'false';n.spellcheck=true;const geom=safeBlockGeometry(page,block);n.style.left=`${geom.x}px`;n.style.top=`${Math.max(0,Number(block.y||0))}px`;n.style.width=`${geom.width}px`;n.style.maxWidth=`${geom.width}px`;n.style.height=`${Math.max(Number(block.height||0),Number(block.fontSizePx||14.67)*1.15)}px`;n.style.fontFamily=block.fontFamily;n.style.fontSize=`${block.fontSizePx}px`;n.style.fontWeight=block.fontWeight;n.style.fontStyle=block.fontStyle;n.style.textDecoration=block.textDecoration;n.style.lineHeight=String(block.lineHeight||1.12);n.style.textAlign=block.textAlign;n.style.whiteSpace=block.whiteSpace||'pre-wrap';n.style.overflowWrap='break-word';n.style.overflow='hidden';n.style.padding='0';n.style.margin='0';n.style.border='0';n.innerHTML=block.html||escapeHtml(block.text);n.addEventListener('focus',()=>revealBlock(page,block,n));n.addEventListener('blur',()=>hideBlock(page,block,n));n.addEventListener('input',()=>{block.dirty=true;block.html=n.innerHTML;block.text=n.innerText.replace(/\u00a0/g,' ');revealBlock(page,block,n);const minH=block.fontSizePx*(block.lineHeight||1.12),nextH=Math.max(minH,n.scrollHeight);if(nextH>block.height){const delta=nextH-block.height;block.height=nextH;shiftLaterBlocks(page,block,delta,{...options,node:n});const mask=page.querySelector(`.glueful-fixed-mask[data-source-block-id="${block.id}"]`);if(mask){mask.classList.add('glueful-fixed-mask-visible');mask.style.height=`${Math.max(block.height,1)+2}px`;mask.style.width=`${Math.min(geom.width,Math.max(block.width,4))}px`}n.style.height=`${nextH}px`}options.onChange?.(block,n)});page.appendChild(n);return n}
function render(model,host,options={}){if(!host)throw new Error('Fixed-page host required.');const valid=M().validate(model);if(!valid.valid)throw new Error(valid.errors.join('; '));host.replaceChildren();css(host);host.style.overflowX='hidden';host.style.overflowY='visible';host.style.width='100%';host.style.boxSizing='border-box';const surface=document.createElement('div');surface.className='glueful-fixed-pages';surface.dataset.renderer='fixed-page-pdf-v5.1';surface.setAttribute('aria-label','Editable fixed-page resume');for(const page of model.pages){const p=document.createElement('section');p.className='glueful-fixed-page';p.dataset.pageId=page.id;p.dataset.pageNumber=String(page.number||1);p.style.width=`${page.widthPx}px`;p.style.height=`${page.heightPx}px`;for(const bg of page.backgrounds||[]){const img=document.createElement('img');img.className='glueful-fixed-bg';img.src=bg.src;img.style.left=`${bg.x}px`;img.style.top=`${bg.y}px`;img.style.width=`${bg.width}px`;img.style.height=`${bg.height}px`;img.style.zIndex=String(bg.zIndex||0);p.appendChild(img)}for(const mask of page.masks||[])renderMask(p,mask);for(const block of page.blocks||[]){if(block.type==='text')renderText(p,{...block,pageWidth:page.widthPx},{...options,pageId:page.id,page})}surface.appendChild(p)}host.appendChild(surface);window.gluefulResumeFixedPageState={model,host,surface,renderer:'fixed-page-pdf-v5.1'};return model}
window.gluefulResumeFixedPageRenderer={render,getState:()=>window.gluefulResumeFixedPageState||null};
})();