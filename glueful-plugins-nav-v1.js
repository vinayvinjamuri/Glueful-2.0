/* Compatibility placeholder only.
 *
 * The authoritative Quick Actions / Plug-ins implementation lives in
 * glueful-jobs-logo-patch-v1.js. This file intentionally contains no runtime
 * handlers, listeners, DOM mutations, or legacy feature flags.
 *
 * Keeping the file as an inert placeholder avoids breaking repositories or
 * historical references that still expect this path to exist while ensuring
 * there is only one active Quick Actions / Plug-ins owner.
 */
