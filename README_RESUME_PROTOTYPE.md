# Resume Studio fixed-page prototype

This branch is intentionally isolated from production. It imports the original PDF as a visual page background and derives editable text rectangles into a canonical model. It is not production-injected by sw.js and must not be merged until browser smoke tests confirm logo, margins, rules, and page geometry against the Word/PDF master.